[project]
name = "trivyignore-cli"
version = "0.1.0"
description = "Build Docker images, scan them with Trivy, and sync findings into .trivyignore.yaml"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.scripts]
trivyignore = "trivyignore_cli.cli:main"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trivyignore_cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
